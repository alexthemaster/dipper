<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Home</title>
    <%- include('partials/head') %>
</head>
<body>
    <div class="container">
        <% if (!loggedIn) { %>
            <div class="alert alert-danger" role="alert">
                You're currently not logged in! Click <a href="/login">here</a> to login!
            </div>
        <% } else { %>
            <nav class="navbar navbar-dark" style="background-color: #4185f2">
                <div>
                    <img src="<%= avatar %>" height="30" style="border-radius:50%">
                    <p class="navbar-text">Logged in as <%= username %> <% if (owner) { %> <%- `(<a href="/admin" target="_blank">Admin Panel</a>)`%> <% }%></p>
                </div>
            </nav>
        <% } %>
        <h2 style="margin-top: 20px;">Currently serving <%= info.users.size %> users (in a total of <%= info.guilds.size %> servers!)</h2>
    </div>
    <%- include('partials/js-bootstrap') %>
</body>
</html>